// SPDX-License-Identifier: CC-BY-4.0
// SPDX-FileCopyrightText: 2026 Jonathan D.A. Jewell (hyperpolymath)
= CappauScripto Blitz Evidence Summary (2026-04-12)
:icons: font

== Run Context

* Target repo: `/var/mnt/eclipse/repos/nextgen-languages/affinescript`
* Evidence directory: `docs/UX-standards/evidence/cappauscripto-blitz-2026-04-12`
* Toolchain:
** `just 1.45.0`
** `dune 3.22.0`
** `ocamlc 5.3.0`

== Command Outcomes

[cols="2,1,4", options="header"]
|===
| Command | Exit | Key evidence

| `just check`
| 1
| `dune fmt --check` unsupported (`unknown option '--check'`).
  See `check.log`.

| `just test`
| 0
| Test suite passed: `173 tests run`.
  See `test.log`.

| `just _blitz-bench`
| 0
| Bench phase reports no bench stanza (`(no bench stanza — skip)`).
  See `bench.log`.

| `dune exec affinescript -- --help`
| 1
| Build fails on warning-as-error:
  `unused value cs_bridge_cmd_fn`.
  See `cli-help.log`.

| `dune exec affinescript -- migrate --dry-run src/`
| 1
| Same warning-as-error failure; migrate path not executable in current state.
  See `migrate-dry-run.log`.

| `just blitz`
| 1
| Fails in `_blitz-build` on warning-as-error (`cs_bridge_cmd_fn`).
  See `blitz.log`.

| `just panic`
| 2
| Recipe invokes `panic-attack assail` without required `<TARGET>`.
  See `panic.log`.
|===

== Primary Blockers

* Lint recipe mismatch: `dune fmt --check` incompatible with installed Dune.
* Compiler warning 32 in `bin/main.ml` breaks build-related commands.
* Bench suite not configured (`@bench` absent).
* Panic recipe missing required target argument.
