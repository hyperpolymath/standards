== T3 Archetype Specs

T3 repos share common proof patterns. Each T3 repo’s spec references ONE
archetype and lists repo-specific items on top.

=== Archetypes

[width="100%",cols="29%,33%,38%",options="header",]
|===
|Archetype |Description |Common Proofs
|link:iser.md[iser] |Code generator (`+*-iser+` repos) |Template
substitution safety, output validity, ABI

|link:a2ml-k9.md[a2ml-k9] |A2ML/K9 parsers & validators |Grammar
completeness, parser termination, serialisation roundtrip

|link:julia-pkg.md[julia-pkg] |Julia packages (`+*.jl+`) |Numerical
bounds, API contracts, type stability

|link:webapp.md[webapp] |Web applications (ReScript) |State machine, XSS
freedom, API contracts

|link:cli-tool.md[cli-tool] |CLI tools |Exit code discipline,
idempotence, input validation

|link:security.md[security] |Security/crypto tools |Key handling, access
control, injection-free

|link:config-infra.md[config-infra] |Config/DNS/infrastructure |Format
validation, idempotence, rollback

|link:game-creative.md[game-creative] |Games/creative |State machine,
scoring correctness

|link:integration-sdk.md[integration-sdk] |SDKs/clients |API compliance,
auth, type safety
|===

=== Mandatory ABI Proofs (ALL T3 repos)

Every T3 repo inherits these from rsr-template-repo: - ABI-1: Non-null
pointer safety - ABI-2: Memory layout correctness - ABI-3: Platform type
sizes - ABI-4: FFI return types - ABI-5: C ABI compliance

See `+~/Desktop/proof-specs/T1-critical/rsr-template-repo.md+` for full
ABI spec.

=== How per-repo specs work

A T3 repo spec looks like:

[source,markdown]
----
# Proof Spec: <repo-name>

**Archetype**: <archetype-name>
**Extra theorems**: N

## Repo-specific

<list of 0-5 bespoke theorems>
----

This gives LLMs everything needed: the archetype tells them the common
proof pattern, the repo-specific list tells them what else to verify.
