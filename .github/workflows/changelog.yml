# SPDX-License-Identifier: MPL-2.0
name: Changelog

on:
  push:
    branches: [main, master]
    paths:
      - 'CHANGELOG.md'

permissions:
  contents: write
  pull-requests: write

jobs:
  generate:
    uses: ./.github/workflows/changelog-reusable.yml
