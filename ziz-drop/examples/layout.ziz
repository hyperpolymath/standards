; SPDX-License-Identifier: MPL-2.0
; The off-side surface. Reads identically to fact.ziz ONCE the layout reader
; lands (tree-sitter external scanner). ziz0 milestone 0 will NOT read this.

define (fact n)
  if (= n 0)
    1
    * n (fact (- n 1))

print (fact 10)
