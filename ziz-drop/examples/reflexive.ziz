; SPDX-License-Identifier: MPL-2.0
; Reflexivity: the environment and the evidence graph are values.

(define x 1)
(define e (current-env))
(print e)

(define (twice f v) (f (f v)))
(print (twice (lambda (n) (* n 2)) 21))

; Ask the graph what it has seen so far (observe mode).
(jeg-dump)
