// SPDX-License-Identifier: PMPL-1.0-or-later
// AffineScript port of RunVectors.res.

module RunVectors;

use VectorRunner;

extern fn throw_message(msg: String) -> a = "error" "throw";

let code = VectorRunner.run();
if code != 0 {
  throw_message("Vector tests failed")
}
