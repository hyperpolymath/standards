{
  "spec_version": "1.0.0",
  "documents": {
    "mustfile": {
      "required_sections": ["Parameters", "Checks"],
      "required_parameters": ["gateway_port", "schema_version"],
      "required_check_fields": ["description", "run"]
    },
    "trustfile": {
      "required_sections": ["Inputs", "Verifications"],
      "required_inputs": [
        "policy_path",
        "policy_hash_path",
        "schema_path",
        "schema_sig_path",
        "schema_pub_path",
        "driver_paths",
        "migrations_path",
        "migrations_sig_path",
        "migrations_pub_path"
      ],
      "required_verification_fields": ["description", "command"]
    },
    "dustfile": {
      "required_sections": ["Logs", "Policy", "Gateway", "Dust-Events"],
      "required_fields_by_section": {
        "Logs": ["path", "reversible", "handler"],
        "Policy": ["path", "rollback"],
        "Gateway": ["event", "undo"],
        "Dust-Events": ["source", "transform"]
      }
    },
    "intentfile": {
      "required_sections": ["Trust-Engine", "Control-Plane", "Pipeline", "Introspection"],
      "required_min_items": 1
    }
  },
  "emission": {
    "type_field": "type",
    "spec_version_field": "spec_version"
  }
}
