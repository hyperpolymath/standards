//! {{ description }}
//!
//! This library provides the core functionality for {{ project-name }}.
//!
//! # Architecture
//!
//! The pipeline is organised into stages:
//!
//! 1. **Ingest** - Read and parse input data
//! 2. **Transform** - Apply processing operations
//! 3. **Validate** - Verify invariants and constraints
//! 4. **Export** - Write output data with checksums
//!
//! # Example
//!
//! ```rust,no_run
//! use {{ project_name }}::{Pipeline, Config};
//!
//! let config = Config::default();
//! let pipeline = Pipeline::new(config);
//! pipeline.run().await?;
//! ```

pub mod config;
pub mod error;
pub mod pipeline;
pub mod checksum;
pub mod validation;

pub use config::Config;
pub use error::{Error, Result};
pub use pipeline::Pipeline;
