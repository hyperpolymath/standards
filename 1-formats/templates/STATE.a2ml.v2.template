# SPDX-License-Identifier: MPL-2.0
# STATE.a2ml v2 template — thin session journal.
# See: standards/a2ml-templates/STATE.a2ml.v2.spec.adoc

@state(version="2.0"):
phase: "{{PHASE}}"
next_action: "{{NEXT_ACTION}}"
last_action: "{{LAST_ACTION}}"
updated: {{DATE}}

@blockers:
# Remove this block if no blockers. Add one @blockers entry per blocker.
# - id: example-blocker-id
#   description: "what is blocked"
#   waiting_on: "external dependency"
#   since: 2026-04-05
@end

@end
